import Pessoas from "../models/pessoas";

class Login {
    async index(req, res) {
    }

    async store(req, res) {
        try {
            const { email, password } = 

            
        } catch (error) {
            
        }

    }
}

export default new Register();